name: ${._name}
_name: tcga_brca${settings.dataset_suffix}
clini_tables: ["${settings.metadata_dir}/TCGA-BRCA_CLINI.csv"]
slide_tables: ["${settings.metadata_dir}/TCGA-BRCA_SLIDE.csv"]
feature_dirs: ["${settings.features_dir}/${.._name}/${settings.feature_extractor}"]
folds_table: "${settings.folds_dir}/${._name}.csv"
filename_col: FILENAME
patient_col: PATIENT