name: ${._name}
_name: cptac_brca${settings.dataset_suffix}
clini_tables: ["${settings.metadata_dir}/CPTAC-BRCA_CLINI.csv"]
slide_tables: ["${settings.metadata_dir}/CPTAC-BRCA_SLIDE.csv"]
feature_dirs: ["${settings.features_dir}/${.._name}/${settings.feature_extractor}"]
folds_table: "${settings.folds_dir}/${._name}.csv"
filename_col: FILENAME
patient_col: PATIENT
