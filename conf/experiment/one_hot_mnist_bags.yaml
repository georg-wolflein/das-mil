# @package _global_
defaults:
  - override /dataset@dataset: one_hot_mnist_bags
  - override /feature_extractor@model.feature_extractor: one_hot

settings:
  feature_size: ${dataset.train.num_digits}
