# @package _global_
defaults:
  - override /dataset@dataset: camelyon16
  - override /feature_extractor@model.feature_extractor: identity

settings:
  feature_size: 768

num_epochs: 20
save_step_freq: 10